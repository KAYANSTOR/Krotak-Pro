#!/usr/bin/env python3
import base64, gzip
from pathlib import Path

# NOTE: full script loaded from local build — if this is still PLACEHOLDER, replace via workflow.
print('PLACEHOLDER_SHOULD_NOT_RUN')
raise SystemExit(1)
