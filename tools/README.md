# tools

`apply_krotak_icon.py` — regenerates all launcher/splash/about icons from `krotak_icon_source.png` (needs Pillow). Outputs are committed; the script is idempotent, so the CI step that runs it changes nothing.
