import 'package:flutter_test/flutter_test.dart';

void main() {
  test('card batch import detailed API deferred', () {
    expect(true, isTrue);
  }, skip: 'importCardsDetailed not in current catalog API');
}
