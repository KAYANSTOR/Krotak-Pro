# تقرير P1 — تقارير فرعية وإعدادات وقوالب

```text
dart analyze lib test  → No blocking issues
flutter test           → All tests passed! (45)
```

## أُغلق وظيفيًا (بيانات حقيقية)

- تقارير مبيعات يوم/شهر + سجل عمليات
- تقرير نقاط البيع (قائمة من المستودع)
- رسائل مرفوضة / معلّقة عبر listByStatus
- إعدادات: SIM، بطارية، قوالب، تصدير CSV، تنظيف+استعادة، تجديد، تفعيل، مساعدة
- TransferTemplateRepository
- Widget smoke tests

## بقي صراحة

- مطابقة بصرية Kotlin الكاملة
- Sale↔POS غير موجود في Domain (لم يُختلق)
- حذف سجلات جماعي غير موجود
- Post-v1 مؤجّل

الشاشات مربوطة ببيانات حقيقية مع حالات تشغيلية أساسية — وليست مطابقة مكتملة لـ Kotlin.
