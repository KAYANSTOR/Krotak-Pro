# خطة بناء واجهات: الإعدادات + مركز المساعدة

| الحقل | القيمة |
|--------|--------|
| **النطاق** | شاشة الإعدادات (بما فيها العناصر الفرعية الظاهرة في المرجع) + مركز المساعدة فقط |
| **المصدر الوصفي** | تقرير الفروقات المرفق (2026) + لقطات Google Drive المرجعية |
| **المنهجية** | PD-2026-09-12-05: لقطة/وصف = مرجع بصري؛ بيانات حقيقية فقط؛ لا تنفيذ جزئي؛ لا تخمين |
| **تاريخ الخطة** | 2026-09-12 |
| **الحالة** | **S0 مُغلقة** — إجابات Q1–Q8 معتمدة في PD-2026-09-12-07 و PD-08؛ جاهز لتنفيذ S1–S3 |
| **خارج النطاق في هذه الخطة** | إدارة المحافظ/نقاط البيع، معالج قوالب فوسك، محاكاة القوالب، قوالب رسائل العملاء والعروض — تُغطى بملف وصفي لاحق |

---

## 1. مصادر الحقيقة (بالترتيب)

1. **لقطات الواجهة المرجعية** (Drive: `13WS6kflQoy3mbr2JR9yoxVFAvc4062xa`) + تقرير الفروقات المرفق.
2. **`docs/product-decisions.md`** — أي قرار معتمد يلغي التعارض.
3. **التنفيذ الحالي في Domain/Data** (ما يعمل فعليًا في المستودع).
4. **`docs/business-rules.md`** و **`docs/net-functional-specification.md`** للسياق الوظيفي العام.
5. **مركز المساعدة الحالي** — محتوى محدود جدًا (5 مواضيع قصيرة)؛ يُوسَّع وفق PD-07 Q8 فقط من مصادر موثّقة.

---

## 2. جرد الحالة الحالية في المستودع

### 2.1 مركز الإعدادات — `SettingsHubScreen`

- ملف: `lib/ui/screens/settings/settings_hub_screen.dart`
- الشكل: قائمة `ListTile` عمودية (9 عناصر) بدون أقسام وكروت ومفاتيح.
- العناصر الحالية: اسم الشبكة، الشريحة، البطارية، قوالب التحويل، تصدير السجل، تنظيف السجلات، تجديد الاشتراك، تفعيل الترخيص، مركز المساعدة.

### 2.2 مفاتيح موجودة

`network_name`, `preferred_sim_slot`, `sms_listen_enabled`, `battery_optimization_acknowledged`, …

**بعد PD-07 تُضاف:** `sms_auto_processing_enabled`, `process_category_amounts_only`, `process_old_messages_on_resume`, `pos_balance_requests_enabled`, `daily_ops_summary_auto_send`, `theme_mode`.

### 2.3 اسم الشبكة / SMS / الاستعادة

كما في الجرد السابق؛ الربط السلوكي للمفاتيح الثلاثة وفق PD-07 وليس بالتخمين.

---

## 3. الهدف البصري (مرجع + PD-07 Q7)

أقسام الصفحة بالترتيب المعتمد:

1. **النظام** — اسم الشبكة + ثلاثة مفاتيح تشغيل
2. **الجهاز والرسائل** — الشريحة + البطارية
3. **المظهر** — فاتح/داكن (متبادل؛ فاتح افتراضي)
4. **نقاط البيع والعمليات** — طلبات الرصيد + ملخص يومي (تفاصيل لاحقًا)
5. **البيانات والقوالب** — قوالب / تصدير / تنظيف
6. **المساعدة** — مركز المساعدة

(التراخيص خارج النطاق الحالي)

---

## 4. المراحل

### S0 — مُغلقة 2026-09-12

الإجابات في **PD-2026-09-12-07** و **PD-2026-09-12-08**.

### S1 — هيكل الإعدادات
عنوان + وصف + أقسام + كروت مشتركة + تحميل/خطأ.

### S2 — كرت اسم الشبكة + Sheet
نص الشرح المعتمد: «سيتم استخدام هذا الاسم في نهاية رسائل SMS المرسلة للعملاء.»

### S3 — المفاتيح + Domain
- تخزين بالمفاتيح أعلاه (افتراضي true / theme light)
- UI Switches
- ربط: معالجة تلقائية مستقلة عن الاستماع؛ مبلغ غير مطابق → معلّقة ثم اعتماد/رفض؛ استعادة عند التشغيل إن ON + idempotency
- اختبارات سلوكية إلزامية

### S4 — تنظيم بقية المداخل

### H1–H2 — مركز المساعدة من مصادر PD-07 Q8 فقط

---

## 5. ترتيب التنفيذ

```text
S0 ✓ → S1 → S2 → S3 → S4 → H1 → H2 → تحديث progress بعد كل مرحلة
```

## 6. معايير القبول

1. أقسام الإعدادات الستة كما PD-07 Q7
2. اسم الشبكة: قيمة + Sheet + نص SMS + حفظ
3. المفاتيح الثلاثة تغيّر السلوك وفق PD-07 (مع اختبارات)
4. لا Mock
5. Help من مصادر موثّقة فقط
6. خارج النطاق: محافظ/فوسك/محاكاة/قوالب عملاء حتى الملف الوصفي التالي

## 7. مراجع

`product-decisions.md` PD-07 / PD-08 · `settings_hub_screen.dart` · `incoming_sms_handler.dart` · `local_message_recovery_service.dart` · `progress.md`
