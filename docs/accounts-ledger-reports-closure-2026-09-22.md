# إغلاق الحسابات / الدفتر / التقارير — 2026-09-22

## ما كان موجوداً مسبقاً (مؤكد بالكود)
- ملف العميل: دفتر حركات + إضافة/خصم رصيد + سبب إلزامي + Audit + UnitOfWork
- اختبار `customer_account_phase3_test` لمسار credit/debit
- PDF: كشف حساب / POS / مبيعات / كروت مباعة
- CSV للكروت المباعة من `SoldCardsService`

## ما أُضيف في هذه الجولة
1. **ترقيم صفحات** لقائمة الحسابات (80 صف + «عرض المزيد» + تمرير)
2. **تصدير CSV** للنتائج المفلترة/البحث من شاشة الحسابات
3. **اختبار** تطابق `getTotalOutstanding` مع `sumCompletedLedger`

## ملاحظات
- مصدر إجمالي الأرصدة في اللوحة يبقى `CustomerBalanceService.getTotalOutstanding`
- لا مسار حذف عملاء
