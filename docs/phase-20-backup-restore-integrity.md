# المرحلة 20 — سلامة النسخ الاحتياطي والاستعادة

**التاريخ:** 2026-09-26  
**الفرع:** `phase-20-backup-restore-integrity` → `main`  
**الأصل:** الخطة الشاملة §20 وفجوة phase-0 (التحقق من SQLite قبل الاستبدال)

## النطاق

- التحقق من توقيع SQLite (`SQLite format 3\0` + عنوان 100 بايت) قبل استبدال ملف قاعدة البيانات الحية.
- كتابة ملف مؤقت ثم إعادة قراءة التوقيع.
- نسخة أمان `.pre-restore` من القاعدة الحية قبل الاستبدال.
- تجاهل مفاتيح `license*` في لقطة الإعدادات.
- استدعاء `reapplyLicenseRows` بعد استعادة قاعدة صالحة.

## خارج النطاق

- تشغيل GitHub Secrets وبناء التوقيع.
- قائمة الجهاز الحقيقي.
- فرع `licensing`.
